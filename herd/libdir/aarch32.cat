(*
 * This file has not been written and is not maintained by ARM Ltd.
 * Authors:
 * Luke Geeson <luke.geeson@cs.ucl.ac.uk>
 *)

AArch32
catdep

let TagCheck = emptyset
let Permission = emptyset
let IC.IVAU = emptyset
let IC.IALLUIS = emptyset
let IC.IALLU = emptyset
let DC.CVAU = emptyset
let TLBInXS = emptyset
let TLBIIS = emptyset
let Q = emptyset
let SVC = emptyset

let DMB.SY = DMB
let DSB.SY = DSB
include "aarch64.cat"
