AArch64 V12
(* Tests load / store immediate unscaled, no offset *)
{ 
0:X0=x; 0:X2=y;
}

 P0                  ;
 MOV X1, #1          ;
 STR X1, [X0, #8]    ;
 LDUR B0, [X0, #8]   ;
 STUR B0, [X2]       ;
 forall (y=1)
 