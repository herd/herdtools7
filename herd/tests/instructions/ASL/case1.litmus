ASL case1

{}

func inv(i::integer) => integer
begin
  case i of 
    when 0: return 1;
    when 1: return 0;
  end
end

func main() => integer
begin
  let a = inv(0);
  let b = inv(1);

  return 0;
end

forall (0: main.0.a = 1 /\ 0: main.0.b = 0)
