ASL while2

{ }

func main() => integer
begin
  var x: integer;
  repeat
    while FALSE do
      let z = 2 ;
    end
    x = 2;
  until x >= 2 ;

  return 0;
end

forall (0:main.0.z=0 /\ 0:main.0.x=2)
