AArch64 A113
(* strh immediate, with offset, symbolic location *)

 { 0:x0=1; 0:x2=x;}

P0;
  STRH W0, [X2, #4];

exists (x=0) (* Not much we can test here, offsetting a symbolic location doesn't make sense*)
