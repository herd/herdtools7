typedef O { prose_description = "O" };

typedef A { prose_description = "A" } =
    | L(O, O)
    { prose_description = "L definition" }
;

typedef B { prose_description = "B" } =
    // L should only be instantiated with two types matching A.
    | (L(O, A, B))
    { prose_description = "L instance" }
;
