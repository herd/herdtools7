typedef O { "O" };

typedef A { "A" } =
    | L(O, O)
    { "L definition" }
;

typedef B { "B" } =
    // L should only be instantiated with two types matching A.
    | (L(O, A, B))
    { "L instance" }
;
