# Building the HTML version of the ASL Reference

## Prerequisite

Install hevea from sources <https://github.com/maranget/hevea>.

## Proceed

Run `sh ./zyva.sh` in this directory. The HTML Reference is in
subdirectory `HTML/`.

