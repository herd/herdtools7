func main() => integer
begin
    // local storage declaration    Time frame
    constant c = 10;                // Constant
    let l = 20;                     // Execution
    var v = 30;                     // Execution
    return 0;
end;
