func main() => integer
begin
    var x : real = 2.0;
    case x of
        when 0.0 => print("2.0");
        otherwise => print("otherwise");
        when 0.0 => print("2.0");
        otherwise => print("otherwise");
    end
    return 0;
end
