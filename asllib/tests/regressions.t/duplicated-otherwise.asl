func main() => integer
begin
    var x : real = 2.0;
    case x of
        when 0.0 => println "2.0";
        otherwise => println "otherwise";
        when 0.0 => println "2.0";
        otherwise => println "otherwise";
    end;
    return 0;
end;
