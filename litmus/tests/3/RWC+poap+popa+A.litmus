C RWC+poap+popa+A
"RfeAA PodRRAP Fre PodWRPA FreAA"
Cycle=RfeAA PodRRAP Fre PodWRPA FreAA
Relax=
Safe=Fre PodWR PodRR RfeAA FreAA
Prefetch=1:x=F,1:y=T,2:y=F,2:x=T
Com=Rf Fr Fr
Orig=RfeAA PodRRAP Fre PodWRPA FreAA

{}

P0 (atomic_int* x) {
  atomic_store(x,1);
}

P1 (volatile int* y,atomic_int* x) {
  int r0 = atomic_load(x);
  int r1 = *y;
}

P2 (volatile int* y,atomic_int* x) {
  *y = 1;
  int r0 = atomic_load(x);
}

exists
(1:r0=1 /\ 1:r1=0 /\ 2:r0=0)
