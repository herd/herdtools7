"CLR Bell"

enum Accesses = 'plain || 'volatile ||
                'acquire || 'release ||
                'lock || 'unlock

enum Fences = 'dep || 'full 

instructions R[{'plain,'volatile,'acquire,'lock}]
instructions W[{'plain,'volatile,'release,'unlock}]
instructions F[{'dep,'full}]
