"Java"

(*definition 1 p. 44*)
enum kinds = 'normal || 'volatile || 'external || 'lock || 'unlock

instructions R[{'normal,'volatile,'lock}]
instructions W[{'normal,'volatile,'unlock,'external}]

let Synchronisation = Volatile | Lock | Unlock 

