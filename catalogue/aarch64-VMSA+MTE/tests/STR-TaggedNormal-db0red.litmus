AArch64 STR-TaggedNormal-db0red
Variant=vmsa,mte,sync
{
 [PTE(x)]=(oa:PA(x),db:0,attrs:(TaggedNormal));
 0:X0=x:red;
}
 P0          ;
 MOV W1,#1   ;
L0:          ;
 STR W1,[X0] ;
exists([x]=1)

(* forall(fault(P0:L0,x,MMU:Permission)) *)
