AArch64 STR-TaggedNormal-v0
Variant=vmsa,mte,sync
{
 [PTE(x)]=(valid:0);
 0:X0=x:green;
}
 P0          ;
 MOV W1,#1   ;
L0:          ;
 STR W1,[X0] ;
exists([x]=1)

(* forall(fault(P0:L0,x,MMU:Translation)) *)
