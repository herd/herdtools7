AArch64 STR-Normal-red
Variant=vmsa,mte,sync
{
 [PTE(x)]=(oa:PA(x),attrs:(Normal));
 0:X0=x:red;
}
  P0          ;
  MOV W1,#1   ;
 L0:          ;
  STR W1,[X0] ;
exists([x]=0)

(* forall([x]=1 /\ ~fault(P0:L0,x)) *)
