AArch64 STR-TaggedNormal-db0
Variant=vmsa,mte,sync
{
 [PTE(x)]=(db:0);
 0:X0=x:green;
}
 P0          ;
 MOV W1,#1   ;
L0:          ;
 STR W1,[X0] ;
exists([x]=1)

(* forall(fault(P0:L0,x,MMU:Permission)) *)
