AArch64 STR-TaggedNormal
Variant=vmsa,mte
{
 [PTE(x)]=(oa:PA(x),attrs:(TaggedNormal));
 0:X0=x:green;
}
 P0          ;
 MOV W1,#1   ;
L0:          ;
 STR W1,[X0] ;
exists([x]=0)

(* forall([x]=1 /\ ~fault(P0:L0,x,TagCheck)) *)
