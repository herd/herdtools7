AArch64 STR-TaggedNormal-red
Variant=vmsa,mte,sync
{
 [PTE(x)]=(oa:PA(x),attrs:(TaggedNormal));
 0:X0=x:red;
}
  P0          ;
  MOV W1,#1   ;
 L0:          ;
  STR W1,[X0] ;
exists([x]=1)

(* forall(fault(P0:L0,x,TagCheck)) *)
