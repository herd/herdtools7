(* diyone7 -arch AArch64 -variant ifetch -metadata false -oneloc DC.CVAUnsRW Rfi *)
AArch64 CoRW1+dc.cvauns-rfi
Variant=ifetch
{
 0:X0=x;
}
 P0          ;
 LDR W1,[X0] ;
 DC CVAU,X0  ;
 MOV W2,#1   ;
 STR W2,[X0] ;

exists (0:X1=1)
